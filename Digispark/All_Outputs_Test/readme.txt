A test sketch for using Digispark in the Arduino IDE.  Sets all six GPIO (0..5) as outputs and cycles them high and low so they can be checked for functionality.
If P5 doesn't cycle, its fuse may be set to use it as a dedicated Reset pin so the fuse will have to be changed to use it as a GPIO.
